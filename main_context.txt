=== 既存のファイル構成 ===
src
src/app
src/app/layout.tsx
src/app/page.tsx

=== 主要なファイルのソースコード ===
--- File: src/app/layout.tsx ---
import React from 'react';

export const metadata = {
  title: 'AI Automated Web System',
  description: 'Generated by Gemini Loop',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="ja">
      <body>{children}</body>
    </html>
  );
}
--- File: src/app/page.tsx ---
import React from 'react';

export default function Home() {
  return (
    <main style={{ padding: '2rem', textAlign: 'center', fontFamily: 'sans-serif' }}>
      <h1>🚀 AI自動開発システム基盤</h1>
      <p>Next.js + TypeScript の環境構築が正常に完了しました！</p>
      <p style={{ color: '#666' }}>Notionからタスクを起票すると、ここにAIが画面を自動生成します。</p>
    </main>
  );
}
--- File: package.json ---
{
  "name": "ai-automated-web-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "vitest run"
  },
  "dependencies": {
    "next": "15.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@google/genai": "^2.8.0",
    "@testing-library/jest-dom": "^6.4.2",
    "@testing-library/react": "^14.2.1",
    "@types/node": "^20.11.24",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^9.0.0",
    "eslint-config-next": "15.0.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.3.1"
  }
}
