"use client";import { useSearchParams } from 'next/navigation';export default function LoginPage() {  const searchParams = useSearchParams();  const userId = searchParams.get('id');  return (    <div className="flex items-center justify-center min-h-screen bg-gray-100 p-4">      <div className="text-center bg-white shadow-lg rounded-lg p-8 md:p-12">        <h1 className="text-3xl md:text-4xl font-bold text-gray-800 mb-4">          ログインID        </h1>        {userId ? (          <p className="text-4xl md:text-5xl font-extrabold text-blue-600">            {userId}          </p>        ) : (          <p className="text-xl md:text-2xl text-gray-600">            IDが指定されていません。          </p>        )}      </div>    </div>  );}